import { UploadFile } from "@/components/UploadFile";
import { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <main>
      <UploadFile />
    </main>
  );
};

export default Home;
